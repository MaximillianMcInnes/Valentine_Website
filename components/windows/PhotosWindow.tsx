export default function PhotosWindow() {
  return (
    <div>
      <h3>📁 Memory Archive</h3>
      <p>Drop your images into <code>/public/photos</code> and we’ll render them.</p>
      <p>(Next step: folder view + gallery.)</p>
    </div>
  );
}